<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ device.name }} - Zephyrus</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 900px;
            margin: 0 auto;
        }
        
        .card {
            background: white;
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            margin-bottom: 20px;
        }
        
        .device-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 30px;
            padding-bottom: 25px;
            border-bottom: 2px solid #f0f0f0;
        }
        
        .device-title h1 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 5px;
        }
        
        .device-id {
            font-family: monospace;
            background: #f5f5f5;
            padding: 5px 12px;
            border-radius: 5px;
            font-size: 0.9rem;
            color: #666;
        }
        
        .device-status {
            padding: 8px 20px;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
        }
        
        .status-active {
            background: #d1fae5;
            color: #065f46;
        }
        
        .status-inactive {
            background: #fee2e2;
            color: #991b1b;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 25px;
            margin-bottom: 30px;
        }
        
        .info-item {
            padding: 15px;
            background: #f9fafb;
            border-radius: 10px;
        }
        
        .info-label {
            font-size: 0.85rem;
            color: #999;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 5px;
        }
        
        .info-value {
            font-size: 1.2rem;
            color: #333;
            font-weight: 600;
        }
        
        .section-title {
            font-size: 1.3rem;
            color: #333;
            margin-bottom: 20px;
            font-weight: 600;
        }
        
        .threshold-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }
        
        .threshold-item {
            padding: 15px;
            background: #fef9c3;
            border: 1px solid #fde047;
            border-radius: 8px;
        }
        
        .threshold-label {
            font-size: 0.85rem;
            color: #854d0e;
            margin-bottom: 5px;
        }
        
        .threshold-value {
            font-size: 1.1rem;
            color: #854d0e;
            font-weight: 600;
        }
        
        .default-badge {
            font-size: 0.75rem;
            background: #d1d5db;
            color: #374151;
            padding: 2px 8px;
            border-radius: 10px;
            margin-left: 8px;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            text-align: center;
            padding: 20px;
            background: #f0f9ff;
            border: 1px solid #bfdbfe;
            border-radius: 10px;
        }
        
        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            color: #1e40af;
        }
        
        .stat-label {
            font-size: 0.85rem;
            color: #1e40af;
            margin-top: 5px;
        }
        
        .actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
            padding-top: 30px;
            border-top: 2px solid #f0f0f0;
        }
        
        .btn {
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.2s;
            border: none;
            cursor: pointer;
            font-size: 0.95rem;
        }
        
        .btn:hover {
            transform: translateY(-2px);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .btn-secondary {
            background: #e0e0e0;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="device-header">
                <div class="device-title">
                    <h1>{{ device.name }}</h1>
                    <span class="device-id">ID: {{ device.deviceId }}</span>
                </div>
                <span class="device-status {{ device.isActive ? 'status-active' : 'status-inactive' }}">
                    {{ device.isActive ? '🟢 Active' : '🔴 Inactive' }}
                </span>
            </div>

            <div class="section-title">Device Information</div>
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Location</div>
                    <div class="info-value">
                        {% if device.location %}
                            📍 {{ device.location }}
                        {% else %}
                            <span style="color: #999;">Not set</span>
                        {% endif %}
                    </div>
                </div>

                <div class="info-item">
                    <div class="info-label">Registered</div>
                    <div class="info-value">{{ device.createdAt|date('M d, Y H:i') }}</div>
                </div>

                <div class="info-item">
                    <div class="info-label">Total Readings</div>
                    <div class="info-value">{{ device.sensorReadings|length }}</div>
                </div>

                <div class="info-item">
                    <div class="info-label">Owner</div>
                    <div class="info-value">{{ device.user.email }}</div>
                </div>
            </div>

            {% if device.description %}
                <div style="margin-bottom: 30px;">
                    <div class="section-title">Description</div>
                    <p style="color: #666; line-height: 1.6;">{{ device.description }}</p>
                </div>
            {% endif %}

            <div class="section-title">Alert Thresholds</div>
            <div class="threshold-grid">
                <div class="threshold-item">
                    <div class="threshold-label">Max Temperature</div>
                    <div class="threshold-value">
                        {{ device.maxTemperature ?? '30.0' }}°C
                        {% if device.maxTemperature is null %}
                            <span class="default-badge">default</span>
                        {% endif %}
                    </div>
                </div>

                <div class="threshold-item">
                    <div class="threshold-label">Min Temperature</div>
                    <div class="threshold-value">
                        {{ device.minTemperature ?? '10.0' }}°C
                        {% if device.minTemperature is null %}
                            <span class="default-badge">default</span>
                        {% endif %}
                    </div>
                </div>

                <div class="threshold-item">
                    <div class="threshold-label">Max CO₂</div>
                    <div class="threshold-value">
                        {{ device.maxCo2 ?? '1000' }} ppm
                        {% if device.maxCo2 is null %}
                            <span class="default-badge">default</span>
                        {% endif %}
                    </div>
                </div>

                <div class="threshold-item">
                    <div class="threshold-label">Max Noise</div>
                    <div class="threshold-value">
                        {{ device.maxNoise ?? '70.0' }} dB
                        {% if device.maxNoise is null %}
                            <span class="default-badge">default</span>
                        {% endif %}
                    </div>
                </div>
            </div>

            <div class="actions">
                <a href="{{ path('device_index') }}" class="btn btn-secondary">← Back to Devices</a>
                <a href="{{ path('device_edit', {id: device.id}) }}" class="btn btn-primary">Edit Device</a>
            </div>
        </div>
    </div>
</body>
</html>